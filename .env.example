# Port to expose Yep Anywhere on (default: 3400)
PORT=3400

# Password for the Yep Anywhere web UI (required)
YEP_PASSWORD=

# Allowed hostnames when behind a reverse proxy (e.g. labrat.example.com)
# Not needed for direct access on localhost
ALLOWED_HOSTS=

# --- Agent API Keys ---
# At least one agent key is needed (or use OAuth for Claude Code via /login)

# Anthropic API key for Claude Code
ANTHROPIC_API_KEY=

# Google API key for Gemini CLI — https://aistudio.google.com/apikey
GEMINI_API_KEY=

# --- Tool & MCP Server Keys ---

# GitHub personal access token — used by the gh CLI
# Create at https://github.com/settings/tokens
GITHUB_TOKEN=

# MCP server API keys (only needed by servers configured in workspace/.mcp.json)
CONTEXT7_API_KEY=
BRAVE_API_KEY=

# --- File-based Secrets ---
# For Docker secrets, Kubernetes, etc., use _FILE variants:
# ANTHROPIC_API_KEY_FILE=/run/secrets/anthropic_api_key
# GEMINI_API_KEY_FILE=/run/secrets/gemini_api_key
# GITHUB_TOKEN_FILE=/run/secrets/github_token
